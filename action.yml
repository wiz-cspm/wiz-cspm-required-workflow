# action.yml
name: 'Git hours'
author: "rjdevnic"
description: 'Git hours action'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  icon: clock
  color: blue
outputs:
  hours:
    description: "The amount of time spent in the repository"
